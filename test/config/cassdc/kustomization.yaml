namespace: deploy-reaper-test

resources:
- cassdc.yaml
- cassdc-non-reaper.yaml

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: reaper-jmx
    literals:
    - username=reaper
    - password=reaper
    type: Opaque
